{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0"
  },
  "homepage":"https://xerohero.github.io/",
  "scripts": {
    "clean-force": "npm cache clean --force",
    "remove-modules": "rm -rf node_modules",
    "install-npm": "npm install --silent --save --save-dev gh-pages react-router@3 --save react-router-dom --save react-facebook-login --save --force",
    "build": "react-scripts build",
    "test": "react-scripts test --passWithNoTests",
    "complete-build" : "npm install --silent node-sass --save --save-dev gh-pages react-router@3 --save react-router-dom --save react-facebook-login --save --force && react-scripts build"

  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
