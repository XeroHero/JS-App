{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0"
  },
  "homepage":"https://xerohero.github.io/JS-App",
  "scripts": {
    "clean-force": "npm cache clean --force",
    "remove-modules": "rm -rf node_modules",
    "install-npm": "npm install --save-dev gh-pages unirest react-router react-facebook-login --save --force",
    "build": "react-scripts build",
    "test": "react-scripts test --passWithNoTests"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
